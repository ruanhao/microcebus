#
# microcebus .gitconfig
#

[alias]
    lg   = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    root = rev-parse --show-toplevel
    co   = checkout
    ci   = commit
    st   = status

[color]
    ui          = true
    status      = auto
    branch      = auto
    diff        = auto
    interactive = auto

[merge]
    tool = vimdiff
